type: object
required:
  - code
  - message
properties:
  code:
    type: integer
    description: HTTP status code representing the error type (e.g., 400 for Bad Request).
    example: 400
  message:
    type: string
    description: A human-readable message providing more details about the error.
    example: "Validation Error: The request data did not pass validation checks."