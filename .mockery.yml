with-expecter: true
dir: "{{.InterfaceDir}}/mocks"
filename: "mock_{{.InterfaceNameSnake}}.go"
outpkg: "mocks"
mockname: "{{.InterfaceName}}"
resolve-type-alias: False
issue-845-fix: True

packages:
  github.com/m4kson/rocket-factory/inventory/internal/service:
    config:
      include-regex: ".*Service"

  github.com/m4kson/rocket-factory/inventory/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/m4kson/rocket-factory/order/internal/service:
    config:
      include-regex: ".*Service"

  github.com/m4kson/rocket-factory/order/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/m4kson/rocket-factory/order/internal/client/grpc:
    config:
      include-regex: ".*Client"

  github.com/m4kson/rocket-factory/payment/internal/service:
    config:
      include-regex: ".*Service"

  github.com/m4kson/rocket-factory/payment/internal/repository:
    config:
      include-regex: ".*Repository"